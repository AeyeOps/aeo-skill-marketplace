# AEO VSC CC Sessions sidecar

Hook sidecar plugin for the AEO VSC CC Sessions VSIX.

The plugin writes per-Claude-process state and event files under
`~/.claude/aeo-vsc-cc-sessions/` and preserves raw hook paths exactly as Claude
reports them.
